## Makefile Template

应用层的项目框架.

[status]
支持源文件自动扫描.
支持C/C++文件混合编译(请记住C文件要extern"C").
支持自建共享库编译.
支持三方库编译.
交叉编译支持

[todo]
[] 添加头文件更改监测, 当更改头文件的时候需要编译相关文件.
[] 对Makefile做第一次抽象, 将相同的规则和共性提取出来.
[] 对Makefile做第二次抽象, 将各种FLAG变量进行规范化.
[] 添加对三方库的自动化处理, 目前三方库的安装prefix需要手动指定到release文件夹中.
